<nav>
  <ul>
    <li><%= link_to "Rambook", root_path %></li>
    <% if !current_page?(new_user_session_path) && !current_page?(new_user_registration_path)%>
      <% if user_signed_in? %>
        <li><%= link_to "Sign Out", destroy_user_session_path(current_user), data: { turbo_method: :delete}%></li>
        <li><%= link_to "Profile", root_path%></li>
      <% else %>
        <li><%= link_to "Sign In", new_user_session_path%></li>
        <li><%= link_to "Sign Up", new_user_registration_path%></li>
      <% end %>
    <% end %>
  </ul>
</nav>
